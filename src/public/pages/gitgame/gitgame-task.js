
/* --- Scripts GitGame --- */

//Obtener el elemento con el id "gitGame" y agregarle la clase "selected"
function addSelected() {
    const gitgame = document.getElementById("gitGame");
    gitgame.classList.add("selected");
}
addSelected();