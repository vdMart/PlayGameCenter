
/* --- Scripts Room --- */

//Obtener el elemento con el id "roomGame" y agregarle la clase "selected"
function addSelected() {
    const room = document.getElementById("roomGame");
    room.classList.add("selected");
}
addSelected();